echo "# Hub-hyundai" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/PedroSanches06/Hub-hyundai.git
git push -u origin main
# Node
node_modules/
.env
.env.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vscode/
.DS_Store

# Prisma
prisma/dev.db
prisma/.env

# Build / logs
dist/
build/
coverage/

# Mac
*.DS_Store
name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [18.x]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Prisma generate
        run: npx prisma generate

      - name: Run lint (if configured)
        run: npm run lint --if-present

      - name: Run tests
        run: npm test --if-present
